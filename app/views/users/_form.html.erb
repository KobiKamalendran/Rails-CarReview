<%= simple_form_for @user do |f| %>
  <%= f.input :email, placeholder: 'user@domain.com', error: 'Username is mandatory, please specify one' %>
  <%= f.input :password, hint: 'Minimum 8 characters.' %>
  <%= f.button :submit %>
<% end %>